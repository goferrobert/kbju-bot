{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Deploy to Fly.io",
            "type": "shell",
            "command": "flyctl deploy",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}